- shell:
  - [git submodule update --init --recursive, Installing submodules]

- clean: ['~']

- link:
    ~/.antigen.zsh: antigen/antigen.zsh
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.z.sh: z/z.sh
    ~/.zshrc: zshrc
